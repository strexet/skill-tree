# Renamed Skill Reference Fixture

Purpose: renamed skill reference case.

Repository state:

- Docs reference legacy skill names without the `skill-tree-` prefix.
- Installed skill directories use `skill-tree-*`.

Expected audit action:

- Replace active docs with prefixed skill identifiers and avoid active legacy aliases.
