# Duplicate Backlog Issue Fixture

Purpose: duplicate backlog issue case.

Repository state:

- `FUTURE.md` has two backlog entries describing the same missing receipt validation gap.
- Evidence and affected paths overlap.

Expected audit action:

- Merge duplicate backlog entries without losing evidence.
